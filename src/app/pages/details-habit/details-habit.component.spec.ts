/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { DetailsHabitComponent } from './details-habit.component';

describe('DetailsHabitComponent', () => {
  let component: DetailsHabitComponent;
  let fixture: ComponentFixture<DetailsHabitComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ DetailsHabitComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(DetailsHabitComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
